# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================
# Este archivo sirve como plantilla. Copia este archivo según el entorno:
# - Para desarrollo local: .env
# - Para Docker: las variables se pasan desde el .env de la raíz vía docker-compose
#
# IMPORTANTE: Las variables se leen en RUNTIME, no durante el build
# ============================================

# ============================================
# DESARROLLO LOCAL (sin Docker)
# ============================================

# Entorno de ejecución
NODE_ENV=development

# Puerto del servidor
PORT=8080

# Base de datos MongoDB (conexión local)
DATABASE_URL=mongodb://localhost:27017/cursala

# URLs del frontend permitidas (CORS)
FRONTEND_DOMAIN=http://localhost:3000,http://localhost:4200
FRONTEND_URL=http://localhost:3000

# ============================================
# CONFIGURACIÓN DE EMAIL (SMTP)
# ============================================
EMAIL_FROM=noreply@cursala.com.ar
EMAIL_HOST=mail.cursala.com.ar
EMAIL_PORT=587
EMAIL_PASSWORD=

# Direcciones de correo específicas
SUPPORT_EMAIL=soporte@cursala.com.ar
ADMIN_NOTIFICATION_EMAIL=admin@cursala.com.ar
NO_REPLY_EMAIL=noreply@cursala.com.ar

# ============================================
# BUNNY CDN (Almacenamiento y Streaming)
# ============================================
BUNNY_STORAGE_CDN_HOSTNAME=https://cursala.b-cdn.net
BUNNY_STREAM_CDN_HOSTNAME=https://vz-19135c35-e7f.b-cdn.net
BUNNY_STORAGE_API_KEY=
BUNNY_STORAGE_ZONE_NAME=
BUNNY_STREAM_API_KEY=
BUNNY_STREAM_LIBRARY_ID=

# ============================================
# DOCKER (estas variables se pasan vía docker-compose)
# ============================================
# NODE_ENV=production
# PORT=8080
# DATABASE_URL=mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@mongo:27017/cursala?authSource=admin
# 
# Las variables EMAIL_*, BUNNY_*, etc. se pasan desde el .env de la raíz
