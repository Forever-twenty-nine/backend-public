
/* Minimal stub test to avoid breaking build while course/certificate behaviors are removed */
describe('course.service stub', () => {
  it('stub test', () => {
    expect(true).toBe(true);
  });
});
